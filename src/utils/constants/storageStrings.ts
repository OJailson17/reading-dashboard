const baseString = '@reading_dashboard';

export const localStorageStrings = {
	BOOK_STATUS: `${baseString}:book_status`,
	CREATE_BOOK_SOURCE: `${baseString}:create_book_src`,
};

export const cookiesStrings = {
	TOKEN: `${baseString}:token`,
	DATABASE_ID: `${baseString}:database_id`,
};
