const baseString = '@reading_dashboard';

export const storageStrings = {
	goals: `${baseString}:goals`,
	auth_token: `${baseString}:auth_token`,
	username: `${baseString}:username`,
	user_id: `${baseString}:user_id`,
};
